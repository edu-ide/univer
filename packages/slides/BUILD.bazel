load("@aspect_rules_js//js:defs.bzl", "js_library")

# Stub package so root npm_link_all_packages can resolve workspace deps during unrelated Bazel builds.
package(default_visibility = ["//visibility:public"])

js_library(
    name = "pkg",
    srcs = [],
    visibility = ["//visibility:public"],
)
